<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container p-2">
    <% if (user) { %>
    <p class="navbar-text text-light me-4">
      Bienvenido, <span class="user"><%= user.name %></span>!
    </p>
    <a class="btn btn-outline-light me-2" href="/logout">Cerrar sesión</a>
    <% } else { %>
    <a class="btn btn-outline-light me-2" href="/login">Iniciar sesión</a>
    <% } %>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item active">
          <a class="nav-link d-flex align-items-center" href="/index">
            <span class="material-icons me-1">home</span>
            Agregar cliente
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link d-flex align-items-center" href="/buscador">
            <span class="material-icons me-1">info</span>
            Buscador de entregas
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link d-flex align-items-center" href="/remitos">
            <span class="material-icons me-1">email</span>
            Generador de remitos
          </a>
        </li>
      </ul>
    </div>
    <!-- Day-Night Mode Toggle Button -->
    <button id="modeToggle" class="btn btn-primary">
      <i class="fas fa-sun d-none"></i>
      <!-- Day Mode Icon (Initially Hidden) -->
      <i class="fas fa-moon"></i>
      <!-- Night Mode Icon -->
    </button>
  </div>
</nav>
